Server Creation = nodejs + Express
-----------------------------------

Basic Commands
---------------

1) Creation of the package.json - npm init
2) Creation of the node modules - npm i express (Installing express)


5) Install mongoose - npm i mongoose

Basic Steps
-------------
1) Creating a folder (Bank server)
2) Create package.json
3) Install express
4) Create index.json
5) Import express inside the index.js
6) Create an app using the express
7) Setup port number
8) Resolving http request
     Get - To get the data
     Post - To post the data
     Put - update data partially
     Patch - update data completely
     delete - To delete data completely
9)HTTP Response Code
   1xx-Information
   2xx-Successful
   3xx-Redirection
   4xx-Client error
   5xx-Server error
10)JWT Token
   Install JWT - npm i jsonwebtoken
   To generate a token - sign()
11)Middleware
It comes in the middle of the request and response cycle of the nodejs execution process
    1)Application specific middleware - For entire application
    req,res,next
    2)Router specific middleware - validate token - verify() -jetwebtoken
12)Database integration
    1)Install mongoose - npm i mongoose
13)Asynchronous function resolving - promise
    1)Resolve state - .then()
    2)Reject state - catch()
14)CORS - Cross origin resource sharing
     install cors - npm i cors
     port 4200(angular) ------------requst---------> 3000(node)(recieve)
15)Asynchronous call - RxJs - (Reactive Extension for Javascript)     

JS - promise
TS - RxJs(library)
    
    1)Observable 
       a)resolved - Subscribe()
       b)reject - catch